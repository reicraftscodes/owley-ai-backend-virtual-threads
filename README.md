#  Spring AI - Model Embedding and Question Answering

This is an ai integration system where one model will be an embedding model and another model will be reasoning model. 

The embedding model will embed pdfs also it will embed pdfs into pinecone database. 

On the other hand when we ask any question to the reasoning model it will use the embedding model to find the answer then from that it will answer the question.


Notes:
Use the sample PDF document located in the Resources folder.